import React from 'react';
import Modal from '../../modal/Modal';

const RecipeForm = (props) => {
  return (
    <div>
      <Modal>
        RecipeForm
      </Modal>
    </div>
  );
};

export default RecipeForm;